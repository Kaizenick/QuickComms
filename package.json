{
  "name": "quickcomms",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "desktop:icons": "tauri icon client/assets/app-icon.svg",
    "desktop:dev": "tauri dev",
    "desktop:build": "npm run desktop:icons && tauri build",
    "desktop:build:windows": "npm run desktop:icons && tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4"
  }
}
